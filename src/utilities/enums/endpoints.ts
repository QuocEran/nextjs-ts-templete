// External link (endpoints api)

const BASE_URL = "https://jsonplaceholder.typicode.com"; // test
// const BASE_URL = "https://jsonplaceholder.typicode.com"; // prod

const Endpoints = {
  BASE_URL: BASE_URL,
  USER: BASE_URL + "/user",
};

export default Endpoints;
